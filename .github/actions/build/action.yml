name: build Kólga
runs:
  using: docker
  image: docker://docker.anders.fi/devops/azure-kolga-demo:master-development
  args: [ "bash", "-c", "./devops create_images && export BUILT_IMAGE=$(./devops docker_test_image) && echo $BUILT_IMAGE && echo '::set-env name=BUILT_DOCKER_TEST_IMAGE::$BUILT_IMAGE'" ]