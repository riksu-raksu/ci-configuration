name: build Kólga
runs:
  using: docker
  image: docker://docker.anders.fi/devops/azure-kolga-demo:master-development
  args: [ "bash", "-c", "./devops create_images && echo '::set-env name=BUILT_DOCKER_TEST_IMAGE::$(./devops docker_test_image)'" ]