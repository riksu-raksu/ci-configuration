name: execute Kólga tests
runs:
  using: docker
  image: docker://docker.anders.fi/devops/azure-kolga-demo:master-development
  args: [ "bash", "-c", "./devops test_setup && cat /github/workspace/tests/registry/certs/docker_registry.crt && make test" ]