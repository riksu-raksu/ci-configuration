name: test action
runs:
  using: docker
  image: docker://docker.anders.fi/devops/azure-kolga-demo:master-development
  args: [ "bash", "-c", "pwd && export && ls -la / && ls -la /app && ls -la /bin" ]